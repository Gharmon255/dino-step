<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <application
        android:name=".WearApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <!--
          Same applicationId as phone (com.gharmon255.dinostep). Install wear APK on watch only.
          Uninstall any previous com.gharmon255.dinostep.wear build before installing.
        -->
        <service
            android:name=".sync.WearDataListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:host="*"
                    android:pathPrefix="/dino_step"
                    android:scheme="wear" />
            </intent-filter>
        </service>

        <service
            android:name=".complication.DinoCreatureComplicationService"
            android:directBootAware="true"
            android:exported="true"
            android:icon="@drawable/ic_complication_egg"
            android:label="@string/complication_label"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE,GOAL_PROGRESS,SMALL_IMAGE,LONG_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="300" />
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.SUPPORTED_TYPES"
                android:value="SHORT_TEXT,RANGED_VALUE,GOAL_PROGRESS,SMALL_IMAGE,LONG_TEXT" />
            <meta-data
                android:name="androidx.wear.watchface.complications.datasource.DEFAULT_CONFIGURATION_SUPPORTED"
                android:value="true" />
        </service>
    </application>

</manifest>
