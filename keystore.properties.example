# Copy to keystore.properties at repo root (gitignored). Never commit the real file or .jks.
#
# Generate keystore once:
#   ./scripts/generate-upload-keystore.sh
#
# Use absolute paths for storeFile.

storeFile=upload-keystores/dino-step-upload.jks
storePassword=REPLACE_ME
keyAlias=dino-step-upload
keyPassword=REPLACE_ME
